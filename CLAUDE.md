# Working conventions

- Commit messages must never include Claude Code attribution, co-author tags, or "Generated with Claude Code" text. Use plain, normal commit messages describing the change.
- After completing each meaningful change (new feature, bug fix, styling update), stage and commit it with a clear, concise commit message describing what changed. Don't commit broken or half-finished work — only commit once the change is complete and working.
